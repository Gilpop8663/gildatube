header 
    div.headerBack
    nav 
        ul.leftBar 
            li 
                a(href="/")
                    i.fab.fa-youtube
        ul.middleBar 
            form(action="/search" method="GET")
                input.searchBar(type="search",name="keyword",placeholder="Search by title")
                button.searchBtn(type="submit" value="")
                    i.fas.fa-search

        ul.rightBar
            if loggedIn 
                li.rightUrl 
                    a(href="/videos/upload")
                        i.fas.fa-file-upload
                li.rightUrl 
                    a(href=`/users/${loggedInUser._id}`) #{loggedInUser.username}의 Profile 
                li.rightUrl  
                    a(href="/users/edit")
                        i.fas.fa-user-edit
                li.rightUrl  
                    a(href="/users/logout") Log Out
            else
                li.rightUrl  
                    a(href="/join") Join
                li.rightUrl  
                    a(href="/login") Login